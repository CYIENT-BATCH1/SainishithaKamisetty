Find empty strings in an array of strings (Array of strings will be given as input to program) 
and print the position of array where empty string is present. And replace the empty string array element as “CYIENT”.

ALGORITHM:
1)Instalize the character variable.
2)Read the inputs
3)Replace the empty string array element as "CYIENT"
4)Position of array where empty string is present
5)print the position of the array and empty string as "CYIENT".
